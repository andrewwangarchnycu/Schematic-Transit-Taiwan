# Public URL of the deployed Cloudflare Worker (see /worker). Not secret --
# this is baked into the static bundle, safe because the Worker itself
# holds the real TDX credentials.
VITE_API_BASE=https://tw-transit-proxy.YOUR-SUBDOMAIN.workers.dev
